{
  "name": "ilovecert",
  "private": true,
  "version": "1.0.0",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "pnpm -r run build",
    "deploy-contracts": "pnpm --filter contracts run deploy"
  }
}
